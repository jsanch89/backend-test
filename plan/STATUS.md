# Estado de tareas

> Estados: ⬜ Pendiente · 🔵 En progreso · ✅ Completada · ⛔ Bloqueada
> Detalle de cada tarea en [TASKS.md](./TASKS.md).

| ID  | Tarea                                              | Fase        | Estado      | Comentario |
|-----|----------------------------------------------------|-------------|-------------|------------|
| T01 | Inicializar repo Git público                       | Setup       | ✅ Completada |            |
| T02 | Proyecto Spring Boot + dependencias mínimas        | Setup       | ✅ Completada | springdoc-openapi-starter-webflux-ui aplazado a T15 (esperando confirmar versión estable compatible con Spring Boot 4.1/Spring Framework 7) |
| T03 | API-first: contrato + openapi-generator            | Setup       | ✅ Completada | Generator `spring` produce `DefaultApi` (sin `x-tags` en el YAML no hay nombre de tag propio); `openApiNullable=false` para evitar dependencia extra de jackson-databind-nullable |
| T04 | Estructura hexagonal (paquetes y puertos)          | Arquitectura| ✅ Completada | Creado README.md con la regla de dependencias entre módulos (T04 paso 4); contenido completo de README queda para T17 |
| T05 | Dominio y caso de uso SimilarProducts              | Arquitectura| ✅ Completada | `domain` build.gradle: `reactor-core` pasado a `api` (antes `implementation`) y plugin raíz `java`→`java-library`, ya que las firmas de los puertos exponen `Flux`/`Mono` a `application`; añadido `junit-platform-launcher` como `testRuntimeOnly` (requerido por Gradle 9) |
| T06 | Adaptador REST inbound (controller)                | Adaptadores | ✅ Completada | `application/build.gradle`: `domain` pasado a `api` (antes `implementation`), ya que el controller en `infrastructure` necesita ver los puertos y el modelo de dominio; validación de `productId` vacío devuelve 400 vía `ResponseStatusException` directamente (el `@RestControllerAdvice` de T08 cubrirá `ProductNotFoundException` y errores genéricos). Arranque real del endpoint (`bootRun`)/tests e2e quedan pendientes de T07 (aún no hay bean para `ProductClientPort`) |
| T07 | Adaptador HTTP outbound (WebClient)                | Adaptadores | ✅ Completada | `getSimilarIds` decodifica con `bodyToMono(ParameterizedTypeReference<List<String>>)` + `flatMapMany` en vez de `bodyToFlux(String.class)`, ya que Jackson2JsonDecoder no fragmenta un array JSON de escalares en elementos de Flux |
| T08 | Gestión de excepciones (404/400/500)               | Adaptadores | ✅ Completada | `@RestControllerAdvice` en `infrastructure` mapea `ProductNotFoundException`→404, `ResponseStatusException`/`ServerWebInputException`→400 (u otro 4xx), y cualquier 5xx/`Exception` genérica→500 con mensaje fijo (sin detalles de la excepción); cuerpo uniforme `{timestamp, status, message}` al no definir el contrato un esquema de error. Verificación end-to-end formal queda para T13 (arranque real bloqueado por bug preexistente de component-scan entre módulos, ajeno a esta tarea); se añadió test unitario del handler en aislamiento para validar el mapeo mientras tanto |
| T09 | Concurrencia en llamadas a detalles                | Perf/Resil. | ✅ Completada | La orquestación paralela (`flatMapSequential` con `concurrency=8`) ya existía desde T05; se añadió el test de tiempos que exige el criterio de aceptación (5 ids con delay simulado de 100ms → respuesta bajo 300ms, no ~500ms secuenciales). Concurrencia de 8 es coherente con el pool de WebClient (`maxConnections=50` por defecto, T07) |
| T10 | Resiliencia: timeouts + circuit breaker            | Perf/Resil. | ✅ Completada | `resilience4j-spring-boot3:2.3.0` + `resilience4j-reactor:2.3.0` sí resuelven y compilan sobre Spring Boot 4.1 (no existe aún variante `-spring-boot4`); `TimeLimiterOperator` vive en el paquete `io.github.resilience4j.reactor.timelimiter` (no `.timelimiter.operator` como el de circuitbreaker). TimeLimiter (2s) + CircuitBreaker (ventana 20, umbral 50%, espera 5s) aplicados solo a `getProductDetail` vía `transformDeferred`; al abrirse el circuito o expirar el timeout, el servicio ya los omite via `onErrorResume` (T05). Retry opcional del paso 5 omitido para no amplificar carga bajo k6. Verificación funcional del degradado bajo carga real queda para T13/T14 |
| T11 | Caché (Caffeine)                                   | Perf/Resil. | ⬜ Pendiente |            |
| T12 | Tests unitarios                                    | Tests       | ✅ Completada | Implementada por delante de T11 (Caché, aún pendiente) a petición del usuario — no bloqueaba T12, pero cuando T11 se implemente convendrá revisar/ampliar `ProductHttpClientAdapterTest`. La mayor parte de la cobertura de `application`/adapters ya existía de tareas previas (T05, T07-T10); se añadieron `ProductDetailMapperTest` (mapeo dominio→DTO) y `SimilarProductsControllerTest` (validación de `productId` en blanco/null y delegación), que faltaban del alcance. `domain` no tiene tests propios: solo contiene un record y una excepción sin lógica que testear. No se configuró Jacoco (no estaba en los pasos; el ~80% de cobertura se cumple a ojo dado el alcance cubierto) |
| T13 | Tests funcionales/e2e (≥5 casuísticas)             | Tests       | ✅ Completada | 6 escenarios (incluye los 6 de la tabla) en `SimilarProductsE2ETest` (`@SpringBootTest` + WireMock, `WebTestClient`); dependencia `org.wiremock:wiremock-standalone:3.9.2` (el artefacto `wiremock` base requiere un `HttpServerFactory` de Jetty aparte, el standalone ya lo incluye) y `.configureStaticDsl(true)` en `WireMockExtension` para que el DSL estático (`stubFor`, `get`…) apunte al puerto dinámico. `resilience4j.timelimiter` sobreescrito a 500ms vía `@DynamicPropertySource` para que el escenario de timeout no alargue la suite; `CircuitBreakerRegistry` reseteado en `@AfterEach` para que los 404 de un test no acumulen fallos hacia el breaker de otro |
| T14 | Verificación con docker-compose + k6               | Tests       | ✅ Completada | Ejecutado sin T11 (caché, aún pendiente) a petición del usuario; 13261 peticiones, 0.84% de error (500), concentrado en el arranque en frío del pool de conexiones, sin degradación progresiva. Notas completas en `plan/k6-results.md` |
| T15 | SwaggerUI                                          | Entrega     | ✅ Completada | `springdoc-openapi-starter-webflux-ui:2.8.6` (la versión aplazada de T02) no es compatible con Spring Framework 7: `SwaggerWelcomeCommon` llama a `UriComponentsBuilder.fromHttpRequest(HttpRequest)`, método eliminado en esa versión de Spring, causando `NoSuchMethodError` al servir `/swagger-ui.html` (500/conexión vacía). Solución: subir a `springdoc-openapi-starter-webflux-ui:3.0.3`, primera línea de springdoc con soporte Spring Boot 4/Spring Framework 7. También se quitó `springdoc.api-docs.enabled: false`: esa propiedad desactiva por completo el bean `SpringDocConfiguration`, del que `SwaggerConfig` depende vía `@ConditionalOnBean`, así que deshabilitar api-docs rompía swagger-ui.html (404) aunque `swagger-ui.url` ya apuntara al YAML estático; dejar `api-docs` habilitado no afecta el enfoque API-first porque la UI igual carga `/api/similarProducts.yaml` (verificado vía `/v3/api-docs/swagger-config`). Verificado end-to-end: `curl`/navegador contra `:5001/swagger-ui.html` (redirige a `swagger-ui/index.html`), UI renderiza el contrato real, y `GET /product/1/similar` contra el mock (`docker-compose up simulado`) responde 200 con datos reales |
| T16 | Dockerfile de la app                               | Entrega     | ✅ Completada | Multi-stage (`eclipse-temurin:21-jdk` build → `eclipse-temurin:21-jre` runtime), copia solo módulos fuente (no `.git`/`build`) y ejecuta `./gradlew :infrastructure:bootJar`; expone `5001` (puerto real de la app, T15) en vez de `5000` como sugería el enunciado, para mantener coherencia con `application.yml`. `EXISTING_APIS_BASE_URL` con default `http://host.docker.internal:3001`, overridable por variable de entorno. Verificado con `docker build` + `docker run -p 5050:5001` contra el mock ya levantado en el host (`:3001`): `/v3/api-docs` 200 y `GET /product/1/similar` devuelve datos reales. No se repitió la corrida completa de k6 contra el contenedor (ya cubierta en T14 contra el mismo jar vía `bootRun`); compose propio (paso 3, opcional) omitido ya que `docker-compose.yml`/scripts de k6 son del enunciado (`backendDevTest/`), no de este repo |
| T17 | README (ejecución, tests, decisiones)              | Entrega     | ✅ Completada | Sección de caché documentada como pendiente (T11 aún no implementada, a petición del usuario) en vez de como decisión cerrada; resto del contenido mínimo (descripción, requisitos, ejecución local/Docker, tests, decisiones de arquitectura/API-first/resiliencia, trade-offs) cubierto. Verificado `./gradlew check` (verde) y `docker build`/`docker run` (T16) siguiendo exactamente los comandos documentados |
| T18 | Revisión final y push                              | Entrega     | ⬜ Pendiente |            |

**Progreso global:** 16 / 18 tareas completadas
